.attached-file id="file-#{attachment.id}"
  = link_to attachment.filename.to_s, url_for(attachment)
  - if can?(:destroy, attachment)
    = link_to 'Delete', attachment_path(attachment), remote: true, method: :delete
