$('#answer-<%= answer.id %> .update-answer-errors').html("<%= render 'shared/errors', resource: answer %>")
<% if answer.errors.blank? %>
  $('#answer-<%= answer.id %>').replaceWith("<%= j render answer %>")
<% end %>
